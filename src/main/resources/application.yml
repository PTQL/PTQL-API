spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema : public
        dialect : org.hibernate.dialect.MySQLDialect
        format_sql : true
  datasource:
    driver-class-name : com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ptqlconstancias2Semestre?useSSL=false&serverTimeZone=UTC
    username: root
    password: mysql
server:
  port: 8080